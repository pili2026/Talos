services:
  talos:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: talos-core
    tty: true
    stdin_open: true
    restart: unless-stopped
    devices:
      - "/dev/ttyMODBUS0:/dev/ttyMODBUS0"

    ports:
      - "8000:8000"

    environment:
      - API_HOST=0.0.0.0
      - API_PORT=8000
      - LOG_LEVEL=INFO
